<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.guying.mapper.UserReviewMapper">

    <select id="getSatisfactionTrend" resultType="com.guying.user.dto.UserSatisfactionTrendDTO$SatisfactionItem">
        SELECT
            DATE_FORMAT(create_time, '%Y-%m-%d') AS date,
            ROUND(AVG(rating), 1) AS avgScore,
            COUNT(*) AS count
        FROM tb_user_review
        WHERE attraction_id = #{attractionId}
          AND status = 1
          AND rating IS NOT NULL
          AND is_deleted = 0
          AND create_time >= DATE_SUB(CURDATE(), INTERVAL (#{days} - 1) DAY)
        GROUP BY DATE_FORMAT(create_time, '%Y-%m-%d')
        ORDER BY date ASC
    </select>

    <select id="selectTotalAvgScore" resultType="java.lang.Double">
        SELECT ROUND(AVG(rating), 1)
        FROM tb_user_review
        WHERE attraction_id = #{attractionId}
          AND status = 1
          AND rating IS NOT NULL
          AND is_deleted = 0
          AND create_time >= DATE_SUB(CURDATE(), INTERVAL (#{days} - 1) DAY)
    </select>

    <!-- 反馈样本：带评分返回，按评分由低到高排序，让低分差评优先进入 AI 建议分析 -->
    <select id="selectFeedbackText" resultType="com.guying.attractions.dto.FeedbackItemDTO">
        SELECT rating, content
        FROM tb_user_review
        WHERE attraction_id = #{attractionId}
          AND status = 1
          AND rating IS NOT NULL
          AND content IS NOT NULL
          AND content != ''
          AND is_deleted = 0
          AND create_time BETWEEN #{startTime} AND #{endTime}
          AND CHAR_LENGTH(content) BETWEEN 5 AND 500
        ORDER BY rating ASC, create_time DESC
        LIMIT 100
    </select>

    <!-- 批量全量聚合：景点列表的平均分与评论数（无时间窗口，按 attraction_id IN 批量查询避免 N+1） -->
    <select id="selectBatchAggregates" resultType="com.guying.attractions.dto.AttractionReviewAggregateDTO">
        SELECT
            attraction_id AS attractionId,
            ROUND(AVG(rating), 1) AS avgScore,
            COUNT(*) AS reviewCount
        FROM tb_user_review
        WHERE attraction_id IN
            <foreach collection="attractionIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
          AND status = 1
          AND rating IS NOT NULL
          AND is_deleted = 0
        GROUP BY attraction_id
    </select>

</mapper>
