<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.guying.mapper.ExperienceAnalysisMapper">
    <select id="getEmotionTrend" resultType="com.guying.ai.dto.EmotionTrendDTO">
        SELECT DATE_FORMAT(create_time, '%m-%d') AS date,
               emotion,
               COUNT(*)                          AS count
        FROM tb_ai_experience_analysis
        WHERE attraction_id = #{attractionId}
          AND create_time >= DATE_SUB(CURDATE(), INTERVAL (#{days} - 1) DAY)
        GROUP BY DATE_FORMAT(create_time, '%m-%d'), emotion
        ORDER BY date
    </select>

    <select id="getEmotionStat" resultType="com.guying.ai.dto.EmotionStatDTO">
        SELECT
            SUM(CASE WHEN create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
                         THEN 1 ELSE 0 END) AS total,
            SUM(CASE WHEN create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
                AND emotion = 0 THEN 1 ELSE 0 END) AS positiveCount,
            SUM(CASE WHEN create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
                AND emotion = 1 THEN 1 ELSE 0 END) AS neutralCount,
            SUM(CASE WHEN create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
                AND emotion = 2 THEN 1 ELSE 0 END) AS negativeCount,
            SUM(CASE WHEN create_time &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
                         THEN 1 ELSE 0 END) AS preTotal,
            SUM(CASE WHEN create_time &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
                AND emotion = 0 THEN 1 ELSE 0 END) AS prePositiveCount
        FROM tb_ai_experience_analysis
        WHERE attraction_id = #{attractionId}
          AND create_time >= DATE_SUB(NOW(), INTERVAL #{doubleDays} DAY)
    </select>

    <select id="getFocusStat" resultType="com.guying.ai.dto.FocusStatDTO">
        SELECT focus, COUNT(*) AS count
        FROM tb_ai_experience_analysis
        WHERE attraction_id = #{attractionId}
          AND create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
        GROUP BY focus
        ORDER BY count DESC
    </select>

    <select id="getWorstFocus" resultType="com.guying.ai.dto.FocusStatDTO">
        SELECT focus, COUNT(*) AS count
        FROM tb_ai_experience_analysis
        WHERE attraction_id = #{attractionId}
          AND emotion = 2
          AND create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
        GROUP BY focus
        ORDER BY count DESC
    </select>

    <!--
        负面样本：直接取冗余存储的 message（被分析的那句原话），focus 与 message 天然一一对应，
        不再 JOIN SPRING_AI_CHAT_MEMORY，彻底消除笛卡尔积与张冠李戴，也不受记忆窗口裁剪影响。
        注意：列改造（新增 message 列）之前的历史数据 message 为 NULL，已用 message IS NOT NULL 过滤。
    -->
    <select id="getNegativeSample" resultType="com.guying.pojo.dto.NegativeSampleDTO">
        SELECT focus, message AS content
        FROM tb_ai_experience_analysis
        WHERE attraction_id = #{attractionId}
          AND emotion = 2
          AND message IS NOT NULL
          AND message != ''
          AND create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
        ORDER BY focus, RAND()
        LIMIT 500
    </select>
</mapper>