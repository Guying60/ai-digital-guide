<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.guying.mapper.FaceEmotionRecordMapper">

    <!--
        面部表情趋势：按景点 + 近 N 天，按日(MM-dd) + 表情(expression) 分组计数。
        SQL 形态与 ExperienceAnalysisMapper.getEmotionTrend 保持一致。
    -->
    <select id="getExpressionTrend" resultType="com.guying.ai.dto.FaceEmotionTrendDTO">
        SELECT DATE_FORMAT(create_time, '%m-%d') AS date,
               expression,
               COUNT(*)                          AS count
        FROM tb_face_emotion_record
        WHERE attraction_id = #{attractionId}
          AND create_time >= DATE_SUB(CURDATE(), INTERVAL (#{days} - 1) DAY)
        GROUP BY DATE_FORMAT(create_time, '%m-%d'), expression
        ORDER BY date
    </select>
</mapper>
