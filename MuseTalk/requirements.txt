# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# FlashHead core (verify the torch / CUDA version matches your RTX 5090 driver)
torch>=2.5.0
torchvision>=0.20.0
diffusers>=0.31.0
transformers>=4.46.0
accelerate>=1.0.0

# Audio I/O (WAV reading for the pipeline)
soundfile>=0.12.0
librosa>=0.10.0

# In-memory JPEG encoding (frame streaming)
Pillow>=10.0.0

# Performance
flash-attn>=2.7.0

# Redis (async status reporting)
redis>=5.0.0,<6.0.0

# ------------------------------------------------------------------
# FlashHead project dependencies
# ------------------------------------------------------------------
# Install the flash_head package itself FIRST:
#   cd /root/autodl-tmp/SX
#   pip install -e .
#
# Then install the deps above.
